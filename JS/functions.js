'use strict';

function subtract(num1, num2) {
    return num1 - num2;
}

let welcome = function welcome(name, age, gender) {
    return `My name is ${name}, I am ${age} years old and I am of gender ${gender}`;
}

let powerUp = (n1, n2) => Math.pow(n1, n2);
